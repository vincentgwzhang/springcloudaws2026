spring.application.name=awsdemo1

# Keep the project runnable as a learning scaffold without local AWS credentials.
# Enable these explicitly per module when you actually need S3 or other AWS services.
spring.cloud.aws.autoconfigure.enabled=false
